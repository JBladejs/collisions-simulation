package com.game_physics.collisions

class Color(red: Int, green: Int, blue: Int) {
    val red = red.toFloat() / 255.0f
    val green = green.toFloat() / 255.0f
    val blue = blue.toFloat() / 255.0f
}